# Core
torch>=2.2.0
transformers>=4.46.0
accelerate>=0.34.0
datasets>=2.20.0
huggingface_hub>=0.24.0

# Retrieval
rank_bm25>=0.2.2
faiss-cpu>=1.8.0        # swap for faiss-gpu if you have CUDA + want faster dense search
sentence-transformers>=3.0.0
open_clip_torch>=2.24.0

# Images / data handling
Pillow>=10.3.0
numpy>=1.26.0
pandas>=2.2.0

# Config management
hydra-core>=1.3.2
omegaconf>=2.3.0

# Eval / plotting
scikit-learn>=1.4.0
matplotlib>=3.8.0
tqdm>=4.66.0

# Optional: only needed if you use GPT-based answer extraction (MRAG-Bench's fallback grading path)
openai>=1.30.0
